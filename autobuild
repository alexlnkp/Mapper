#!/bin/bash

_BINDIR="bin"

_CSTANDART="c99"
_CFLAGS="-Wall -Wextra -Wpedantic -O3 -std=${_CSTANDART} -lraylib -lm"

_EDITOR_SOURCES="src/editor.c"

mkdir -p ${_BINDIR}
gcc -s ${_EDITOR_SOURCES} ${_CFLAGS} -o ${_BINDIR}/editor

wc -c ${_BINDIR}/editor